package com.maulik.toastx

/** Vertical motion for toast enter/exit. */
enum class ToastEnterAnimation {
    /** Slides up from below the slot (default). */
    FromBottom,
    /** Slides down from above the slot. */
    FromTop,
}
